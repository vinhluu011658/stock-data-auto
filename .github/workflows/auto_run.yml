name: Auto Run Daily

on:
  workflow_dispatch:

jobs:
  run_daily:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
          pip install requests gspread oauth2client

      # 👉 chạy price pipeline trước
      - name: Run price_pipeline.py
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python price_pipeline.py

      # 👉 sau đó chạy nha_dau_tu.py
      - name: Run nha_dau_tu.py
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python nha_dau_tu.py

      # 👉 sau đó chạy rate.py
      - name: Run rate.py
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python rate.py

      # 👉 sau đó chạy foreign_trade.py
      - name: Run foreign_trade.py
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python foreign_trade.py

      # 👉 sau đó chạy info_stock.py
      - name: Run info_stock.py
        env:
          GOOGLE_CREDENTIALS: ${{ secrets.GOOGLE_CREDENTIALS }}
        run: python info_stock.py
        
      - name: Done
        run: echo "Workflow hoàn tất"
